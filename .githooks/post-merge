#!/usr/bin/env bash
set -euo pipefail

REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"

if ! git rev-parse --verify ORIG_HEAD >/dev/null 2>&1; then
  exit 0
fi

git diff --name-status ORIG_HEAD HEAD | .githooks/lib/refresh-codex-artifacts
